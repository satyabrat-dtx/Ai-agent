# DB2ADMIN.SKETCHDETAILSIZE

- **Module**: `OTHER` (none confidence — no known prefix matched)
- **Roles**: `business_data`
- **Columns**: 12
- **Primary key**: `SKETCHDETAILSKETCHCOMPANYCODE`, `SKETCHDETAILSKETCHCODE`, `SKETCHDETAILSKETCHITEMTYPECODE`, `SKETCHDLTSKETCHPRDUNIQUEID`, `SKETCHDLTSKETCHTYPEDETAILCODE`, `SKETCHGROUPCODE`, `SIZESIZESTYPECODE`, `SIZECODE`
- **FK degree**: referenced by 0 constraint(s), references 1 constraint(s)
- **Source**: `DB2ADMIN_DDL.sql` line 216393

## Columns

| # | Column | Type | Null | Key | Tags | Meaning |
|---|--------|------|------|-----|------|---------|
| 0 | `SKETCHDETAILSKETCHCOMPANYCODE` | CHAR(3) | NOT NULL | PK FK | primary_key foreign_key |  |
| 1 | `SKETCHDETAILSKETCHCODE` | CHAR(10) | NOT NULL | PK FK | primary_key foreign_key |  |
| 2 | `SKETCHDETAILSKETCHITEMTYPECODE` | CHAR(3) | NOT NULL | PK FK | primary_key foreign_key |  |
| 3 | `SKETCHDLTSKETCHPRDUNIQUEID` | BIGINT | NOT NULL | PK FK | primary_key foreign_key |  |
| 4 | `SKETCHDLTSKETCHTYPEDETAILCODE` | CHAR(10) | NOT NULL | PK FK | primary_key foreign_key |  |
| 5 | `SKETCHGROUPCODE` | CHAR(10) | NOT NULL | PK | primary_key |  |
| 6 | `SIZESIZESTYPECOMPANYCODE` | CHAR(3) | NOT NULL |  |  |  |
| 7 | `SIZESIZESTYPECODE` | CHAR(3) | NOT NULL | PK | primary_key |  |
| 8 | `SIZECODE` | CHAR(10) | NOT NULL | PK | primary_key |  |
| 9 | `MEASURE` | DECIMAL(5,2) |  |  |  |  |
| 10 | `TOLERANCEPERCENTAGE` | DECIMAL(5,2) |  |  |  |  |
| 11 | `ABSUNIQUEID` | BIGINT | NOT NULL |  | surrogate_id | Framework-assigned surrogate row id (BIGINT). Present on most tables. NO foreign key in this schema references it, but it is the target of the implicit FATHERID parent link. Not part of the primary key. |

## References (this table → parent) — 1

| Constraint | Local columns | → Table | → Columns | ON DELETE | JOIN predicate |
|---|---|---|---|---|---|
| `SKETCHDETAIL_GROUPSIZE` | `SKETCHDETAILSKETCHCOMPANYCODE`, `SKETCHDETAILSKETCHCODE`, `SKETCHDETAILSKETCHITEMTYPECODE`, `SKETCHDLTSKETCHPRDUNIQUEID`, `SKETCHDLTSKETCHTYPEDETAILCODE` | [`SKETCHDETAIL`](../OTHER/SKETCHDETAIL.md) | `SKETCHCOMPANYCODE`, `SKETCHCODE`, `SKETCHITEMTYPECODE`, `SKETCHPRODUCTUNIQUEID`, `SKETCHTYPEDETAILCODE` | RESTRICT | `SKETCHDETAILSIZE.SKETCHDETAILSKETCHCOMPANYCODE = SKETCHDETAIL.SKETCHCOMPANYCODE AND SKETCHDETAILSIZE.SKETCHDETAILSKETCHCODE = SKETCHDETAIL.SKETCHCODE AND SKETCHDETAILSIZE.SKETCHDETAILSKETCHITEMTYPECODE = SKETCHDETAIL.SKETCHITEMTYPECODE AND SKETCHDETAILSIZE.SKETCHDLTSKETCHPRDUNIQUEID = SKETCHDETAIL.SKETCHPRODUCTUNIQUEID AND SKETCHDETAILSIZE.SKETCHDLTSKETCHTYPEDETAILCODE = SKETCHDETAIL.SKETCHTYPEDETAILCODE` |

## Referenced by (child → this table) — 0

_None._

## Indexes

- `SKETCHDETAILSIZEUID` (ABSUNIQUEID)

## Starter query

```sql
SELECT t.SKETCHDETAILSKETCHCOMPANYCODE,
       t.SKETCHDETAILSKETCHCODE,
       t.SKETCHDETAILSKETCHITEMTYPECODE,
       t.SKETCHDLTSKETCHPRDUNIQUEID,
       t.SKETCHDLTSKETCHTYPEDETAILCODE,
       t.SKETCHGROUPCODE,
       t.SIZESIZESTYPECOMPANYCODE,
       t.SIZESIZESTYPECODE,
       t.SIZECODE,
       t.MEASURE,
       t.TOLERANCEPERCENTAGE,
       t.ABSUNIQUEID
FROM   DB2ADMIN.SKETCHDETAILSIZE t
FETCH FIRST 100 ROWS ONLY;
```
