# DB2ADMIN.PARTIALLOCATION

- **Module**: `WAREHOUSE` (low confidence — FK neighbourhood: 1 of 1 related tables are WAREHOUSE)
- **Roles**: `business_data`
- **Columns**: 8
- **Primary key**: `PRFLOCPHYWAREHOUSECOMPANYCODE`, `PRFLOCPHYSICALWAREHOUSECODE`, `PRFLOCATIONTEMPLATECODE`, `PRFLOCALLOCATIONTEMPLATECODE`, `WAREHOUSEZONECODE`
- **FK degree**: referenced by 0 constraint(s), references 2 constraint(s)
- **Source**: `DB2ADMIN_DDL.sql` line 1225

## Columns

| # | Column | Type | Null | Key | Tags | Meaning |
|---|--------|------|------|-----|------|---------|
| 0 | `PRFLOCPHYWAREHOUSECOMPANYCODE` | CHAR(3) | NOT NULL | PK FK | primary_key foreign_key |  |
| 1 | `PRFLOCPHYSICALWAREHOUSECODE` | CHAR(8) | NOT NULL | PK FK | primary_key foreign_key |  |
| 2 | `PRFLOCATIONTEMPLATECODE` | CHAR(3) | NOT NULL | PK FK | primary_key foreign_key |  |
| 3 | `PRFLOCALLOCATIONTEMPLATECODE` | CHAR(3) | NOT NULL | PK FK | primary_key foreign_key |  |
| 4 | `WAREHOUSEZONECODE` | CHAR(3) | NOT NULL | PK FK | primary_key foreign_key |  |
| 5 | `PARTIALLOCATIONCODE` | CHAR(10) |  |  |  |  |
| 6 | `WHSZONEPHYWHSCOMPANYCODE` | CHAR(3) | NOT NULL | FK | foreign_key |  |
| 7 | `ABSUNIQUEID` | BIGINT | NOT NULL |  | surrogate_id | Framework-assigned surrogate row id (BIGINT). Present on most tables. NO foreign key in this schema references it, but it is the target of the implicit FATHERID parent link. Not part of the primary key. |

## References (this table → parent) — 2

| Constraint | Local columns | → Table | → Columns | ON DELETE | JOIN predicate |
|---|---|---|---|---|---|
| `PREFERENTIALLOCATION_PARTIALLOCATION` | `PRFLOCPHYWAREHOUSECOMPANYCODE`, `PRFLOCPHYSICALWAREHOUSECODE`, `PRFLOCATIONTEMPLATECODE`, `PRFLOCALLOCATIONTEMPLATECODE` | [`PREFERENTIALLOCATION`](../WAREHOUSE/PREFERENTIALLOCATION.md) | `PHYSICALWAREHOUSECOMPANYCODE`, `PHYSICALWAREHOUSECODE`, `TEMPLATECODE`, `ALLOCATIONTEMPLATECODE` | RESTRICT | `PARTIALLOCATION.PRFLOCPHYWAREHOUSECOMPANYCODE = PREFERENTIALLOCATION.PHYSICALWAREHOUSECOMPANYCODE AND PARTIALLOCATION.PRFLOCPHYSICALWAREHOUSECODE = PREFERENTIALLOCATION.PHYSICALWAREHOUSECODE AND PARTIALLOCATION.PRFLOCATIONTEMPLATECODE = PREFERENTIALLOCATION.TEMPLATECODE AND PARTIALLOCATION.PRFLOCALLOCATIONTEMPLATECODE = PREFERENTIALLOCATION.ALLOCATIONTEMPLATECODE` |
| `WAREHOUSEZONE_WAREHOUSEZONE` | `WHSZONEPHYWHSCOMPANYCODE`, `PRFLOCPHYSICALWAREHOUSECODE`, `WAREHOUSEZONECODE` | [`WAREHOUSEZONE`](../WAREHOUSE/WAREHOUSEZONE.md) | `PHYSICALWAREHOUSECOMPANYCODE`, `PHYSICALWAREHOUSECODE`, `CODE` | RESTRICT | `PARTIALLOCATION.WHSZONEPHYWHSCOMPANYCODE = WAREHOUSEZONE.PHYSICALWAREHOUSECOMPANYCODE AND PARTIALLOCATION.PRFLOCPHYSICALWAREHOUSECODE = WAREHOUSEZONE.PHYSICALWAREHOUSECODE AND PARTIALLOCATION.WAREHOUSEZONECODE = WAREHOUSEZONE.CODE` |

## Referenced by (child → this table) — 0

_None._

## Indexes

- `PARTIALLOCATIONUID` (ABSUNIQUEID)

## Starter query

```sql
SELECT t.PRFLOCPHYWAREHOUSECOMPANYCODE,
       t.PRFLOCPHYSICALWAREHOUSECODE,
       t.PRFLOCATIONTEMPLATECODE,
       t.PRFLOCALLOCATIONTEMPLATECODE,
       t.WAREHOUSEZONECODE,
       t.PARTIALLOCATIONCODE,
       t.WHSZONEPHYWHSCOMPANYCODE,
       t.ABSUNIQUEID
FROM   DB2ADMIN.PARTIALLOCATION t
FETCH FIRST 100 ROWS ONLY;
```
